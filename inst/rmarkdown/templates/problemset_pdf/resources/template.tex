\documentclass[11pt,a4paper]{article}
\usepackage[top=1.3cm,bottom=2cm,left=2cm,right=2cm]{geometry}

\usepackage[immediate]{silence}
\WarningFilter[temp]{latex}{Command} % silence the warning
\usepackage{sectsty}
\DeactivateWarningFilters[temp] % So nothing unrelated gets silenced
\sectionfont{\fontsize{12}{15}\selectfont}
\subsectionfont{\fontsize{10}{12}\selectfont}
\allsectionsfont{\sffamily} % Set font of section

\usepackage{calc}
%%%%% FONTS
\RequirePackage{microtype} % this makes fonts almost imperceptibly smoother
\RequirePackage{droidserif} % Droid Serif font for body
\RequirePackage[T1]{fontenc} % requires XeLatex or LuaTex (remove to use pdfLaTex)
%\RequirePackage[utf8]{inputenc} % ignored when using XeLaTex or LuaTex (uncomment when using pdfLaTex)
\RequirePackage{fontspec} % requires XeLatex or LuaTex (remove to use pdfLaTex)
% For the headings we will use Helvetica
\RequirePackage{helvet}
\usepackage[immediate]{silence}
\WarningFilter[temp]{latex}{Command} % silence the warning
\usepackage{sectsty}
\DeactivateWarningFilters[temp] % So nothing unrelated gets silenced
\allsectionsfont{\sffamily}
% For the main text and equations we will use Baskerville and Palatino (Old version)
% \RequirePackage{mathpazo}
% \RequirePackage{baskervald}
% For formatting code or package names, we will use Lucida Console
\RequirePackage{zi4} %Lucida console TypeWriter Font
% Enable strikethrough
\usepackage[normalem]{ulem}
% \setmainfont{Arial}
%\usepackage{lmodern}
%\usepackage{ebgaramond}
%\usepackage{amsmath}
%\usepackage{amsfonts}
%\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{longtable}
\usepackage{hanging}
\usepackage[english]{babel}
\usepackage{tikz} % Draw things
\usepackage{ltablex} % Multipage tables
\usepackage{float} % Positioning figures
\usepackage{subcaption}
\usepackage{wrapfig} % Wraps text around figures
\usepackage{array}
% \usepackage{booktabs}
\usepackage{fancyhdr} % Fancy headers and footers
\pagestyle{fancy}
\counterwithin{table}{section}
\usepackage{tocloft}
\usepackage{booktabs}
\usepackage{csquotes}
\usepackage{biblatex}
\usepackage{mathtools}
\usepackage[normalem]{ulem}
\usepackage{enumitem}
\usepackage{amssymb,amsmath}
\usepackage[
  colorlinks  = true,
  urlcolor    = blue,
  citecolor   = red,
  linkcolor   = red % Sets the color of links in general
]{hyperref}

% Settings
\author{$authors$}
\date{$date$}
\lhead{$authors$}
\rhead{$course$: $title$}
\addbibresource{$bibliography$} % Set path to bibfile
\setlength{\parindent}{4em} %Identation paragraphes
\setlength{\parskip}{1em} % Interligne paragraphes
\renewcommand{\baselinestretch}{1.0} %Interligne
\interfootnotelinepenalty=10000 %% Completely prevent breaking of footnotes
\setlength{\headheight}{25pt} % Headheight
\numberwithin{equation}{section} % Numbering equations

\renewcommand{\headrulewidth}{0.5pt} % Line delemiting the header
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecfont}{\itshape}

% Define CSL environment
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{CSLReferences}%
{\setlength{\parindent}{0pt}%
\everypar{\setlength{\hangindent}{\cslhangindent}}\ignorespaces}%
{\par}

%Define float placement.
\makeatletter\renewcommand*{\fps@figure}{H}\makeatother

% Define standard fig width, need the graphix package for that
\setkeys{Gin}{width=.75\textwidth}

% Define Shaded environment for correct knitting
$if(highlighting-macros)$
$highlighting-macros$
$endif$

\nocite{*}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

\thispagestyle{plain}
\title {\textbf{$course$: $title$}} 
\author {$authors$}
\maketitle
\pagenumbering{arabic}
\makeatletter
\renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
  \hskip -\arraycolsep
  \let\@ifnextchar\new@ifnextchar
  \array{#1}}
\makeatother

$body$

\end{document}